# Example extra metadata for GLS loggers A dataset containing example extra metadata for GLS loggers.

Example extra metadata for GLS loggers A dataset containing example
extra metadata for GLS loggers.

## Usage

``` r
example_extra_metadata
```

## Format

An object of class `data.frame` with 1 rows and 5 columns.
