# Example metadata for GLS loggers A dataset containing example metadata for GLS loggers.

Example metadata for GLS loggers A dataset containing example metadata
for GLS loggers.

## Usage

``` r
example_metadata
```

## Format

An object of class `data.frame` with 1 rows and 6 columns.
